# VF-71 — automatic phone sync

The September 8 task fixed the Mac firewall, stale address recovery, authenticated
rediscovery and sync merging. Those changes were present in this workspace and
installed on the Mac, but had not been committed. Its device check forced the
periodic job to run; that proved the job could sync, not that new captures or
network recovery would trigger delivery automatically.

## Established failure paths

- BubbleService retried only pending **audio** on network return/startup. Once a
  dictation was transcribed, an empty audio queue returned before any sync call,
  even if the finished transcript was still unsynced.
- Saving a local dictation or continuation did not schedule delivery. If its
  immediate in-process sync failed or never ran, it depended on the periodic
  job, which was registered only by MainActivity and could be deferred by Android.
- A shared `stopped` flag in the old JobService could let a stopped attempt
  finish a replacement attempt when a new start reset that flag.

The pre-fix Android build reproduced the first two gaps in the isolated QA app:
save a finished transcript without an Activity, then start the bubble with no
queued audio. Neither path uploaded it. The production Samsung diagnostics are
still needed to attribute the user's specific September 12 incident completely.

## Repair

Local dictation/chat writes now schedule persisted delivery immediately, with
expedited scheduling when available, a normal-job fallback, and exponential
retry starting at 30 seconds. New edits replace that job, preserving another
attempt when an edit races an upload. Each JobService run owns its completion;
stopped runs cannot finish a newer run.

Bubble startup/connectivity now syncs finished transcripts independently of its
audio queue, resets discovery cooldown on a new network, and ensures the
periodic catch-up job exists. Sync failure cannot interrupt transcript delivery.
Redacted diagnostics record the trigger, pending count, result and timing.

## Validation

- Android debug and isolated syncQa builds pass; all 8 merge/identity unit tests pass.
- Production Swift sync-server test passes identity/auth rejection, malformed
  request handling, synchronous upsert and concurrent retry deduplication.
- Android 14 emulator: 10 checks pass without opening MainActivity, directly
  calling SyncClient, or forcing a JobScheduler run. The QA driver saves through
  the production Store and exits its process; Android runs delivery itself.
- Synthetic capture delivered at 09:54:42 UTC; its continuation at 09:54:49.
- An upload failed during the simulated endpoint outage, remained unsynced,
  then Android retried and delivered it at 09:55:35 without a manual action.
- Finished transcript with empty audio queue recovered at bubble startup at
  09:55:41. Existing bubble recovered another transcript after both emulator
  transports were disabled and Wi-Fi restored, at 09:55:52. All were acknowledged.
- Current installed Mac: firewall enabled, block-all disabled, Voice Flow allowed;
  `/sync-probe` returns HTTP 200 over loopback and the Mac's Wi-Fi address.
- Production manifest retains a private BubbleService and contains no QA driver
  or receiver. QA uses a separate package, files and Keystore.

Machine-readable receipts: `vf71-fixed-2026-09-12.json` and
`vf71-baseline-2026-09-12.json`. These are targeted checks, not release-gate evidence.

Ready-to-install production APK:
`android/app/build/outputs/apk/debug/app-debug.apk`

SHA-256: `019573a9f4c6ebf7cbc758e798113c45fced7bb189c1fe1a578acdf23c8910db`

## Remaining physical-phone check

The phone was not connected to adb during this work. The APK has **not** been
installed on Safet's phone. Connect/unlock it over USB, collect current job,
battery/standby and sync diagnostics, install the production APK preserving data,
and verify a real new dictation plus Wi-Fi interruption/recovery with the app
closed. Samsung scheduling delays and the exact reported incident remain
unverified. VF-71 should stay Waiting on Safet until that check is possible.
