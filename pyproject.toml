[project]
name = "voice-flow"
version = "0.1.0"
description = "API speech-to-text dictation and streaming speech playback"
requires-python = ">=3.11,<3.13"
dependencies = [
    "numpy>=1.24.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
